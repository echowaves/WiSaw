// import { Reducer, } from 'redux-testkit'
// import * as photosListReducer from './reducer'

describe('photosList reducer', () => {
	it('should have initial state', () => {
		// expect(reducer.initialState).toEqual({
		// 	photos: [],
		// 	loading: false,
		// 	errorMessage: '',
		// 	thumbnailMode: true,
		// })
		expect(2).toEqual(2)
	})
})
