## 1. Update Drawer Label

- [x] 1.1 Replace string `drawerLabel` with a render function that returns a View with two Text components in `app/(drawer)/_layout.tsx`
- [x] 1.2 Show "Waves" as the primary Text and upload target wave name as a second Text with smaller font size and reduced opacity
- [x] 1.3 Add `numberOfLines={1}` to the wave name Text for ellipsis truncation on long names
- [x] 1.4 When no upload target is set, render only the "Waves" Text with no second line
