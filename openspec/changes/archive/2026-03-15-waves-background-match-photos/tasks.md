## 1. Background Fix

- [x] 1.1 In `src/screens/WavesHub/index.js`, change the container backgroundColor from `theme.BACKGROUND` to `theme.INTERACTIVE_BACKGROUND` to match PhotosList

## 2. Verification

- [x] 2.1 Verify `src/screens/PhotosList/index.js` container uses `theme.INTERACTIVE_BACKGROUND` (no change needed, just confirm match)
