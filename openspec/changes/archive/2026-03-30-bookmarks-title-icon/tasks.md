## 1. Add bookmark icon to header title

- [x] 1.1 In `src/screens/BookmarksList/index.js`, create a `headerTitle` const that renders a `<View>` with `flexDirection: 'row'` containing an Ionicons `bookmark` icon (size 18, color `theme.TEXT_PRIMARY`) and a `<Text>` with "Bookmarks"
- [x] 1.2 Replace all 4 `<AppHeader title='Bookmarks' ...>` calls to use `title={headerTitle}` instead

## 2. Verification

- [x] 2.1 Verify the component renders without errors
