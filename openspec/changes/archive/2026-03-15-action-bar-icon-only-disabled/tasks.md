## 1. Hide Text Labels on Disabled Action Buttons

- [x] 1.1 Report button: conditionally render the "Report" text only when the button is enabled
- [x] 1.2 Delete button: conditionally render the "Delete" text only when the button is enabled
- [x] 1.3 Star button: conditionally render the "Star"/"Starred" text only when the button is enabled
- [x] 1.4 Wave button: conditionally render the wave label text only when the button is enabled (isOwnPhoto)
- [x] 1.5 Share button: conditionally render the "Share" text only when the button is enabled
- [x] 1.6 Make disabled (icon-only) buttons round: override minWidth and borderRadius in actionButtonDisabled so the button becomes a circle matching the height
