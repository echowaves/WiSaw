## 1. Conditional Rendering

- [x] 1.1 In `src/components/IdentityHeaderIcon/index.js`, add early return of `null` when `hasIdentity` is true (nickName is not empty), before the existing render logic

## 2. Verification

- [x] 2.1 Verify the header layout in `src/screens/PhotosList/components/PhotosListHeader.js` does not need adjustment (absolute-positioned wrapper should be unaffected)
- [x] 2.2 Confirm drawer identity menu item in `app/(drawer)/_layout.tsx` remains unchanged and functional
