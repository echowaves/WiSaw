## 1. Fix Declaration Order

- [x] 1.1 In `src/screens/WaveDetail/index.js`, move the `handleDeleteWave` function definition above the `headerMenuItems` array so the `onPress` reference is valid at construction time
