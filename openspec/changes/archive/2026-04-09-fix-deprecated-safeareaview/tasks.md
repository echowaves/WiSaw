## 1. Update AppHeader SafeAreaView import

- [x] 1.1 In `src/components/AppHeader/index.tsx`, remove `SafeAreaView` from the `react-native` import and add it to the existing `react-native-safe-area-context` import

## 2. Update TandC SafeAreaView import

- [x] 2.1 In `src/screens/TandC/index.tsx`, remove `SafeAreaView` from the `react-native` import and add it to the existing `react-native-safe-area-context` import

## 3. Verify

- [x] 3.1 Confirm no remaining `SafeAreaView` imports from `react-native` in the codebase
