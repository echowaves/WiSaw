## No Spec Changes

This change fixes a code bug (missing `waveUuid` in `captureArgs`) without modifying any requirements. The existing `photo-upload` spec already correctly describes the expected behavior.
