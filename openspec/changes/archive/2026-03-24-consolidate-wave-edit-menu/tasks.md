## 1. WaveDetail Screen

- [x] 1.1 Replace `rename` and `edit-description` menu items with single `edit-wave` item in `headerMenuItems` array in `src/screens/WaveDetail/index.js`

## 2. WavesHub Screen

- [x] 2.1 Replace `rename` and `edit-description` menu items with single `edit-wave` item in `contextMenuItems` array in `src/screens/WavesHub/index.js`, using existing `handleEditWave` handler
