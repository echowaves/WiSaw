## 1. PhotosList Camera Capture

- [x] 1.1 Add "Cancel" button to `checkPermission()` Alert in `src/screens/PhotosList/hooks/useCameraCapture.js` — insert `{ text: 'Cancel', style: 'cancel' }` before the "Open Settings" button

## 2. WaveDetail Camera Capture

- [x] 2.1 Add "Cancel" button to camera permission denied Alert in `src/screens/WaveDetail/index.js` — insert `{ text: 'Cancel', style: 'cancel' }` before the "Open Settings" button
- [x] 2.2 Add "Cancel" button to photo library permission denied Alert in `src/screens/WaveDetail/index.js` — insert `{ text: 'Cancel', style: 'cancel' }` before the "Open Settings" button
