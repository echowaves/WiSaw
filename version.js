// Shared version and build configuration
// This file is the single source of truth for version and build numbers

const APP_VERSION = '7.2.5'
const BUILD_NUMBER = 335

module.exports = {
  APP_VERSION,
  BUILD_NUMBER,
}
